# OSC 2026 项目申报书：MoonDICOM

- **项目名称**：MoonDICOM：MoonBit 医学影像数据解析与隐私保护工具
- **项目性质**：原创开源项目
- **项目仓库**：https://github.com/CCllff-jpg/MoonDICOM-MoonBit-.git
- **项目作者**：CCllff-jpg

## 项目摘要
MoonDICOM 是一个纯 MoonBit 实现的 DICOM Part 10 医学影像元数据工具包，面向科研、教学、数据集整理和跨机构交换场景，提供 DICOM 文件解析、元数据质量校验、隐私脱敏、审计记录、DICOM JSON 导出和命令行检查能力。

## 背景与目标
DICOM 文件除影像数据外还包含患者姓名、患者编号、检查日期、医院和医生等敏感元数据。现有数据在科研和教学使用前需要被准确读取、检查和脱敏。本项目旨在提供轻量、可复用、跨后端的 MoonBit 基础库，降低医学影像元数据处理门槛。

## 用户场景
科研人员可解析和检查公开数据集；教学人员可脱敏后制作演示数据；数据工程人员可通过 CLI 批量前置检查元数据；MoonBit 开发者可复用二进制读取器、DICOM 数据模型和 JSON 转换接口。

## 核心功能
1. 支持 Part 10 前导区、DICM 标识、Explicit VR Little Endian 和基础 Implicit VR Little Endian 解析。
2. 支持 Tag、VR、文本、常见整数和原始字节值，以及有界读取和偏移诊断。
3. 支持 UID、日期、图像尺寸、位深、像素表示和必要元数据质量校验。
4. 支持患者字段删除、文本替换、私有标签移除和脱敏审计，且不修改源数据集。
5. 支持 DICOMweb 风格 JSON 导出及 `inspect`、`validate`、`convert`、`anonymize` CLI 命令。

## 技术方案与创新点
项目使用 MoonBit 编写，以类型安全的 `Tag`、`ValueRepresentation`、`DataElement`、`DataSet` 建模 DICOM 元数据；通过有界 little-endian 读取器避免越界读取，并用稳定问题码生成校验报告。项目将二进制协议解析、医学影像隐私保护和 MoonBit 跨后端能力结合，形成差异化的数据基础设施工具。

## 实施计划与当前进展
已完成项目骨架、Tag/VR 字典、有界二进制读取器、Part 10 解析器、数据集模型、质量校验、脱敏、JSON 导出、native CLI、测试和文档。后续计划包括更完整的 Tag 字典、Sequence 支持、UID 稳定映射、日期偏移、Markdown/CSV 报告、批量扫描、CI 和 Wasm 本地查看器。

## 预期成果
形成可复用的 MoonBit DICOM 元数据库、命令行工具、测试样例和完整文档；为医学影像科研与教学数据处理提供安全的基础能力，并推动 MoonBit 在医学数据和二进制协议工具方向的生态应用。

## 开源合规、风险与限制
项目采用 Apache-2.0 许可证，当前实现和测试使用合成数据，不包含真实患者资料或未授权第三方代码。项目不涉及医学诊断，不承诺所有 DICOM VR、完整 Sequence、压缩像素解码或 DICOM 网络服务；当前脱敏仅处理元数据，不检测像素内烧录文字，不生成二进制脱敏 DICOM，也不替代人工审核、机构安全策略和法规合规流程。
